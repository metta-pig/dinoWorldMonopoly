# Assumptions

Record gaps when `game-input/brief.md` is incomplete.

- Default presets from `.env.example` apply until brief overrides them.
- Games are for casual / fun use — no formal licensing workflow in v1.
