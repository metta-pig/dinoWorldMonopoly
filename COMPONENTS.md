# Components

| Component | Count | Notes |
|-----------|-------|-------|
| Example card | 52 | SVG template `assets/print/templates/card-front.svg` |
| Example token | 20 | SVG circles, cut lines |

## Print specs (SVG only)

- Card size: 63.5 × 88 mm (2.5 × 3.5 in) — adjust in SVG `viewBox`
- Include 3 mm bleed if printing professionally; safe margin 4 mm for home printers
- Export paths under `assets/print/generated/` (gitignored)

## Digital-only

When `digital-arcade` or deliverable is `digital-web` only, mark N/A for physical rows.
